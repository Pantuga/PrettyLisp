(define factorial ["n"] <
  (if {n .< 0} (return "undefined"))
  (if {n == 0} (return 1))
  (return {n*(factorial {n - 1})})
>)
{in := (parse_num (input))}
(println "The factorial of " in " is:")
(println (factorial in))