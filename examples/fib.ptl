(define fib ["n"] <
  (if {n .< 0} (return "undefined"))
  (if {n == 0} (return 0))
  (if {n == 1} (return 1))
  {result := {(fib {n - 1}) + (fib {n - 2})}}
  (return result)
>)
{in := (parse_num (input))}
(println "The " in "th fibonacci number is:")
(println (fib in))